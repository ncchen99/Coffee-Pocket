# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Mapbox
VITE_MAPBOX_TOKEN=

# Add-cafe FastAPI service (services/api). 本地開發用 http://localhost:8000;
# 正式環境填入部署後的 URL。
VITE_ADD_CAFE_API_BASE=http://localhost:8000

# OAuth — 由 Supabase Auth 後台設定,前端不直接持有 secret
