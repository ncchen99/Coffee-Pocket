
# TODO

- [x] 電腦版網頁的咖啡廳詳細資訊區塊右上角叉叉按鈕需改為常駐顯示，不要隨著向下捲動而消失，避免無法點擊；目前為半透明，開始捲動後希望改為實心顯示。
- [x] 修正電腦版 scrollbar（滾動條）的樣式問題。
- [x] Safari 上面 scrollbar 有變成細細長長的樣式，但是區塊的右留白變多了，導致左右留白不對稱，這個需要修正。
- [x] 至於第二點，在 Google Chrome 上面則沒有套用這種客製化的 scrollbar 樣式。
- [x] 新增戶外座位標籤
- [x] 修正主頁搜尋標籤「不限時」與「有插座」外框顏色不一致的問題
- [~] [調查後發現不花錢好像沒辦法] 希望修改 Google 登入的授權名稱，不要使用 Supabase 的 URL，目前這樣不太好看，不知道可不可以修改？ 
- [X] 希望電腦頁面上咖啡廳的詳細資訊區塊寬度可以再縮
- [x] 修正爬蟲問題：部分咖啡廳的圖片抓到了評論者的頭像
- [X] 在 Mapbox 的地圖上，我發現當前的位置會被圖釘給覆蓋（不確定是不是地圖上的 Z-index 設置問題），導致看不清楚
- [X] 希望 Mapbox 地圖可以新增一個按鈕，點擊後能讓地圖視角回到現在的定位位置
- [x] 新增 AI 摘要的部分，並修正目前評論摘要抽取資訊不正確的問題。
   (a) 譬如「台南-CHADOR」這一個，標籤應該要有「適合讀書」、「適合聊天」、「不限時」及「多人」9982dfde-4c88-430f-b63c-786ff4ab3b16 
   (b) 目前這些標籤卻都沒有出現，這部分需要修正
- [x] 修正現在最多隻能顯示 3 公里以內的店家的問題。不知道出了什麼狀況，像是「腐城」這一家就一直顯示不出來。
希望預設進入首頁時，應該要把這 200 多家店都顯示出來才對。
- [x] 增加新增標籤的功能
現在沒有新增標籤的功能，應該要讓使用者可以新增標籤。
- [x] 檢查是否有 function 可以移除 改成前端直接連線 supabase 處理 透過好的 rls 規則達到一樣的安全性，但是更好的效率
- [x] 搜尋可以直接用關鍵字搜尋 可以輸入咖啡廳的名子 （詞向量搜尋？） 成本太高的話 用類似 big query??? 或是直接用 google 地圖的搜尋 API，但這個會不會貴貴啊？ postgreSQL FTS + 同義詞/拼字容錯 或是直接做這個就好了，或是直接用這個內建的功能，或是一些 plugin 來做，這樣好像會便宜很多，但不確定 Supabase 有沒有資源。
- [x] 前端標籤投票後 不要按百分排序 跳動影響體驗
- [x] 修正 new row violates row-level security policy for table "cafe_tags" 看怎麼設計
- [x] 口袋名單的頁面 清單中元素的標題和 rating 沒有垂直置中，且 rating 和星星太小
- [x] 咖啡廳 slug 希望咖啡廳點擊之後，這個詳細資訊會展開瀏覽器的連結可以是這個 slug。如果現在沒有的話，是不是需要到資料庫再寫入這個欄位？
那這張資料表要改，會不會有一些衝突？如果沒有衝突的話，幫我寫到這個 Cafe 的資料表。不過這可能要用 LLM 去轉成 slug，或者是用什麼方式去轉？
- [x] Onborading 位置授予完或點擊中西區後 應該進入主頁 且跳過沒有反應
- [x] 更改圖標或是清點擊後的預設 zoom 大小 大概 16
- [x] 當詳細資訊區塊或是進階篩選出現的時候，Mapbox 的地圖上面覆蓋一個模糊的圖層；等到詳細資訊區塊出現完畢，再將這個模糊的圖層淡出。 關閉的時候也是，用來解決地圖跳動的問題
- [x] 修改 input 當中的 placeholder 顏色 目前灰色的和主題不搭
- [x] 修正結果清單的時間說明不準：點開後顯示「已休息」，而實際時間也確實已打烊，但搜尋結果這邊顯示營業中。 現在剛過營業時間 不確定這個線索有沒有幫助
- [x] 口袋名單頁面優化：
    希望顯示的咖啡廳資訊能更豐富，除了名稱之外，還需要加入：
    1. 圖片
    2. 標籤
    3. 營業狀態（是否正在營業）
    4. 評分（星星等級）

    因此口袋名單的排版可能需要再思考如何修改。
- [x] 希望搜尋完之後，如果再點擊其他標籤或者是快速場景，底下的搜尋狀態就要清掉:找到 1 間店名 / 地址含「腐城」
- [x] 目前的口袋名單請求都很慢，需要修正
- [x] 前端的 API 程式是不是有地方可以優化？

關於優化的原則如下：

1. 減少請求次數
   一個工作如果需要兩三次請求才能完成，我們是否可以改成一次？或是思考如何調整做法，這是其中一個優化的準則。

2. 做法的優化
   尋找更快速、更好的做法，以節省 Supabase 的資源並增加使用者的體驗。

- [x] 修正「口袋名單」頁面點擊「在地圖上看」時，無法在地圖上正常顯示對應口袋的問題。
- [x] 小小的 UX 體驗修正：現在進入到「我」或者是「口袋」的這兩個頁面，都會先顯示「請登入」，然後再跳出 Skeleton，然後才是內容。這個會有閃爍，應該是先出現 Skeleton 才對。
- [x] 個人頁面的「我的貢獻」這一個部分一直都跑不出來啊
- [x] 搜尋完之後，如果再點擊其他標籤或者是快速場景，底下的搜尋狀態就要清掉:找到 1 間店名 / 地址含「腐城」 現在已經會清掉。但我希望這一個輸入框右邊的按鈕狀態也會復原。
搜尋之後會顯示「清除」，那這時候如果點了其他標籤或是快速場景——我知道了，輸入框內容不用清掉，但是右邊的按鈕狀態要復原，改回「搜尋」。
- [x] 單純的店家搜尋和 tag 比對，是不是可以搬到前端來做？因為第一次進入頁面時，其實就會取得這 200 多家咖啡廳的資料，這時候存到 Local Storage 應該可以在前端直接做查詢。
所以像是拼音比對、字串比對搜尋的這些演算法，也就可以搬到前端來執行，但後端的這些功能應該還是可以保留。這樣速度會加快很多，畢竟咖啡廳就只有 200 多間。
而且這樣也可以做到 real-time 的輸入，讓底下的搜尋結果即時更新。如果使用者輸入完後沒有看到他要的，按 Enter 才會跑 AI 處理，或是按了 Enter 之後才跑搜尋或 AI。這樣會不會好一些？
- [x] 有機會一定要把這個 Mapbox 寬度改變就會畫面閃爍、重新 Render 的問題修掉。
- [x] 沒有登入的話，就不要顯示「標籤與證據」這邊的「讚」與「倒讚」按鈕，因為沒有登入就不能使用這些功能。
- [x]另外像是「加入口袋」和「回報問題」等功能，或是使用者在未登入狀態下，「新增標籤」等相關選項是不是也都不要顯示？

這樣統一處理比較不會誤導用戶。
- [x] 這個投票、按讚或倒讚的功能好像壞掉了。

具體問題如下：
1. 按讚之後，「幾則證據」的數字不會更新。
2. 新增標籤時，前端畫面不會馬上跳出新標籤，必須重新整理之後才會出現。
3. 點擊已按讚的標籤（表示要刪除該新增標籤）時，系統也不會執行刪除。

這些部分需要修正一下。


- [x] Pipeline 的程式要修改一下，目前有幾個結構需要更新：

1. 時間限制（Time Limit）
   應該要有三種狀況：
   (a) 不限時 
   (b) 限時(多久?)
   (c) 客滿時才限時（否則不限時）(多久?)

2. 插座（Outlets）
   狀況也很多種，需要區分：
   (a) 全部座位都有插座
   (b) 多數座位有插座
   (c) 少數座位有插座
   (d) 沒有插座

3. 寵物友善（Pet Friendly）
   目前的定義怪怪的，因為 AI 幾乎都是從評論判斷是否有「店貓、店狗」，但這不代表店家就是寵物友善。由於 LLM 容易搞錯，乾脆將此項直接改成「是否有店貓」、「是否有店貓店狗」，顯示上的說明文字要再調整，避免語句太長。

4. 新增另外四個欄位：
   (a) 桌子大小：
       狀況包含：全部都是大桌子、大部分是大桌子、少數大桌子，或完全沒有大桌子。
   (b) Wi-Fi：確認是否有提供。
   (c) 預算（Budget）：
       這部分應該就是指 CP 值，我在想或許可以用一個分數來呈現。
   (d) 停車（Parking）：
       這也有多種可能，需區分：不好停車、機車好停車、汽車好停車，或是都好停車。或是就分成兩個標籤：
1. 機車好停車
2. 汽車好停車

然後以「是否好停」的來紀錄呢？ 在想這些解答應該都要用「是」跟「否」會比較容易，但像插座和桌子這種可能會有不同情況，或是可以考慮將它們標籤化。

譬如說「插座」，可以分成這兩個標籤：
1. 多有插座
2. 少數插座

「桌子」也可以採用類似的想法來處理。
對於「適合多人」這個標籤，基準應該要在 4 人以上。雖然要從評論中篩選出這個資訊難度較高，但我認為還是可以做這個標籤。

只是 LLM 必須很仔細地去判斷，內容是否真的提到適合「多個人」，甚至是用數字明確講出可以提供 4 人以上的聚會，標籤才會成立；而不是隻要提到「適合跟朋友聊天」就可以。

原因在於很多咖啡廳雖然適合聊天，但其實不接受 4 人或 5 人以上的群體，因為店內沒有那麼大的座位。這就是我們需要注意的一些小細節。
- [x] 134-xiang-nei-de-nv-wu-s現在聚合顯示標籤的邏輯好像有問題。
像是這一家咖啡廳，點開之後的詳細資訊裡面，標籤與證據並沒有「寵物友善」，但是在搜尋結果這邊，它卻有「寵物友善」的標籤，而且點擊「寵物友善」標籤時它也會出現，這個部分感覺怪怪的。

- [x] 感覺爬蟲需要改一下評論抓取的邏輯，改抓「最相關」的而不是最新的。
應該抓最相關的評論，然後數量可能抓 200 筆左右。 但在 AI 評論聚合這邊（也就是聚合 AI 的摘要），可能要思考一下，如果有 200 筆，要如何一次餵給它？會不會有資料量太多的問題？
目前也在思考，不能只靠一筆證據就決定它是成立的，可能要看總共的證據數量。這筆證據在總共的證據比例中佔多少？如果比例很低，那可能是 AI 誤判、評論者搞錯，或者是他寫的文字並沒有那個意思。
所以這部分也要想一下怎麼處理，是不是要設個 threshold？
- [x] 發現手機上面的進階搜尋是有問題的，它的指定時間搜尋仍然不是新版的，而且在搜尋結果這邊還出現了一個時間篩選的小區塊，這些都還是舊版的內容。
不確定為什麼電腦版已經改到了，但手機版卻沒有。
- [x] 口袋和個人頁面重新整理之後，設定的主題顏色會跑掉，但是點回首頁又會有主題顏色
- [x] 修改地圖的顯示，讓地圖的視角鎖定 鎖定地圖的垂直顯示，並移除地圖上顯示立體的元素，因此立體效果產生的陰影也會一併移除
- [!] 手機版本的全面測試，思考是否要優化 UI/UX
- [x] 手機的咖啡廳詳細資訊檢視頁面應該要修改，並合併到搜尋結果中。

具體的 UI/UX 優化建議如下：

1. 採用 Bottom Sheet 設計
   (a) 應該要做成可拖曳的 Bottom Sheet，這樣使用者可以同時查看地圖與咖啡廳資訊。
   (b) 當使用者點擊搜尋結果列表時，原本列表的位置會直接切換成顯示咖啡廳的詳細資訊，將這兩個頁面合併在一起。
   (c) 當 Bottom Sheet 往上拖曳時，會進入詳細資訊或搜尋列表的全螢幕模式。

2. 返回機制（Back Management）
   (a) 透過網址 Router 來捕捉使用者的 Back 行為(捕捉系統的 Back 行為)，點選返回時會從詳細資訊回到列表頁。
   (b) 目前規劃先以系統的 Back 功能為主，但可以再思考是否需要在畫面上加入額外的「X」關閉按鈕或「Back」按鈕。這個部分想請你幫我想一下，一般在設計上會怎麼做，畢竟這個是網頁，也不是真的 App。
並且因為改成像是 bottom sheet 這樣子的顯示詳細資訊，所以想請你幫忙將這個餐廳封面圖，移到「AI 摘要」和「分享、地圖開啟、加入口袋」這兩個區段的中間。

將這個封面圖移到這邊，要等到可能它上滑才會看清楚
所以手機上當點擊地圖上的圖標時，也會直接開啟那個咖啡廳的詳細資訊頁面，其實就是底下的 bottom sheet 內容換掉這樣子
想請你幫我做一個細微的設計。當使用者點擊地圖，也就是地圖在 Focus 的狀態下，bottom sheet 的高度改成剩 30%，讓地圖放大顯示；當然，他可以再把 bottom sheet 拉上來，高度就會變成 70%。

我目前的構想如下：

1. 搜尋結果列表：
   bottom sheet 的高度固定為 30% 或 70%。

2. 詳細資訊頁面：
   (a) 當使用者點進詳細資訊時，預設高度為 50%。
   (b) 若使用者點擊地圖，高度會縮減至 30%。
   (c) 若點擊底下 bottom sheet 的詳細資料區塊，或是手動拉上來，則顯示 70%、90% 甚至 100%（這部分的最終比例可以交給你決定，看怎麼做會更好）。

其實這個操作邏輯就是參考 Google Maps 的做法。
- [x] 手機的咖啡廳詳細資訊頁面，這個 header 文字如果太長，不會自動在後面變成省略符號（...），所以會超出顯示畫面的寬度。
- [x] 部署到 Firebase
- [x] 不論是在手機還是電腦的頁面上，搜尋結果中咖啡廳顯示的標籤，以及詳細資訊上方顯示的那三個重點標籤，我想請你幫我隱藏掉「高 CP 值」這個選項。

因為這個選項比較主觀，我不希望將它作為主要的數據來呈現。但在下方的詳細資訊中，還是可以保留顯示它是否屬於「社群評論高 CP 值」；主要是在搜尋結果與上方的摘要部分，請不要顯示這個標籤。謝謝你。

- [x] 注意到現在前端和資料庫的標籤好像有點對不起來。

因為標籤有更新了，所以前端好像有蠻多標籤的處理都還沒有改到。想請你幫忙修改前端的程式，謝謝。

- [x] 現在這個搜尋框點擊之後，會正常出現搜尋區塊，這個設計是好的。但因為搜尋框原本顯示在地圖上時有較重的陰影，導致點擊後元件看起來像是浮起來的，設計上較不一致。

- [x] 我想請你幫我修正這個問題：
1. 當出現搜尋區塊的時候，請幫我把搜尋框的陰影移除。
2. 另外，讓元素浮起來的陰影設計本身是好的，目前定位按鈕和搜尋框都有這個效果。我希望搜尋框下方的四個快速標籤也能加入陰影設計，讓它們同樣浮在地圖上面。
3. 同樣地，當搜尋區塊出現時，也請將這四個快速標籤的陰影移除。
4. 現在這個 bottom sheet 可能是因為 padding-top 的樣式有改，導致搜尋結果列表顯示的 title 位置沒有在區塊內垂直居中。

譬如說「附近的好咖啡」這一個文字，我發現好像只有它是沒有垂直對齊的。它的樣式和我們點擊搜尋之後出現的結果列表上方標題（bottom sheet 上方的標題）是不一樣的樣式。

這部分請幫我修正，謝謝。
- [x] 另外，目前已經有做到：在搜尋結果列表點擊一家咖啡廳時，地圖會滑到對應的位置；點擊叉叉後，也會回到剛才搜尋結果的畫面狀態（也就是地圖會回到原本的位置）。

但現在發現一個問題：
當我們點擊一家咖啡廳後，接著在地圖上點擊旁邊的另外一家咖啡廳（這時使用者可能是想查看鄰近咖啡廳的狀況），看完後再點擊叉叉，畫面卻會停在那裡，地圖沒有回到原本搜尋列表顯示的位置。

這部分想請你幫我修正，謝謝。

- [x] PWA + icon
1. 希望地圖在操作雙指縮放時，底下的 bottom sheet 也可以同步收合（可能縮到 30% 左右）。
   目前的邏輯是點擊地圖時 bottom sheet 會收下去，希望加入「雙指縮放」也能觸發收合的機制。

2. 關於登入頁面的設計：
   在未登入的狀況下，點擊「口袋」或「個人」會跳出登入頁面。目前該頁面是使用右上角的「X」關閉，想要改成在左上角使用「Back」返回鍵的設計。